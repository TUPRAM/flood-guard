# Superseded planning documents

Historical record. **Nothing here is a live plan.** `PLANS.md` at the
repository root is the single current roadmap.

Kept rather than deleted because these documents record *why* the early
structure was chosen, which is not recoverable from the code alone.

| Path | What it was | Superseded by |
| --- | --- | --- |
| `tasks/codex_backlog.md` | Original Codex-ready backlog | `PLANS.md` |
| `tasks/task_01_repo_scaffold.md` … `task_06_dashboard.md` | Per-task briefs for the initial six milestones (scaffold, FPPS score, road disruption, access loss, equity gap, dashboard) | `PLANS.md`, and the implementations in `src/floodguard/` |

Moved from `tasks/` on 2026-07-29 (audit D-27) so the repository has one
roadmap rather than two.
