# Candidate replication resources and recovery

Checkpoint: 23 September 2026. **PASS for three source-bound study finals, the staged eight-case EvidenceLibrary build, its independent public verifier and the 24-file repository copy; PARTIAL for exact-release verification; second-machine reproduction NOT RUN.** All paths below are logical paths under the configured external release stage, not product-code defaults. The current stage is the new `proposal-release-staging-v1` research run under the project-support execution root. On another computer, reconstruct or copy only the portions covered by their verified source, finals and export manifests; do not infer that this laptop's absolute path exists there.

## Machine and runtime

The local run used Windows 11 Home Single Language, version `10.0.26200`, 64-bit; AMD Ryzen 7 6800H (8 physical cores, 16 logical processors); and 16,312,393,728 bytes of physical RAM (15.19 GiB). The three finals receipts bind CPython 3.12.13, GDAL/ogr2ogr 3.13.0, rasterio 1.5.0 / GDAL 3.12.1, pyproj 3.7.2, shapely 2.1.2, networkx 3.6.1 and NumPy 2.5.0. Their runtime identity SHA-256 is `a128e8eb3cd0b8817b2f75bb7236aba91feea250588e4e684d46d090df9ccb34`, with `uv.lock` SHA-256 `b78500d58a624601b7bcf2cd10f6d7a9ba2e32cdb91586d3ed3f99e3a223630f`. This is one measured configuration, not a validated minimum system requirement.

The resource monitor recorded process working set about every five seconds in `qa/finals_resource_samples.csv` (SHA-256 `cccebcf66ad868c9b8fa9cd9f6ccd01935b9b1f8592cd94c7f777d8fb48f97dc`). Sampling began after the parallel AOI-03/AOI-05 processes launched, but before AOI-06 launched. Therefore the maxima below are **sampled maxima, never exact process peaks**. They exclude child-process and system-wide simultaneous memory unless included in the sampled process working set; do not use them as a guaranteed RAM ceiling.

| Case | Command launch metadata UTC | Receipt write UTC | Approx. launch-to-receipt wall time | Monitor samples | Sampled maximum working set | Final directory disk size | Final build receipt SHA-256 |
| --- | --- | --- | ---: | ---: | ---: | ---: | --- |
| Hat Yai core, AOI-03 | 12:56:29.381 | 13:07:11.992 | 10 min 43 s | 81 | 1.712 GiB | 612,238,923 bytes (583.9 MiB; 13 files) | `07fd80b7efd031619845231e9d09c80f42adc27d61e80841b236a77a0e794dc5` |
| Bang Ban/Sena, AOI-05 | 12:56:29.052 | 13:07:44.175 | 11 min 15 s | 88 | 3.327 GiB | 1,215,284,519 bytes (1,159.0 MiB; 15 files) | `ce4c781120465bc56057eb6f7e0e64854126fa1c4bf36c3d3dfed35bd728833a` |
| Rangsit, AOI-06 | 13:07:29.786 | 13:24:05.341 | 16 min 36 s | 185 | 4.392 GiB | 1,565,823,725 bytes (1,493.3 MiB; 15 files) | `0be8cb624b7fc6d344465f75d88e6f0685edd193a585860ea11268c8308f7b38` |

The operator evaluated `--generated-at (Get-Date -AsUTC -Format o)` in PowerShell immediately at each process launch. The table subtracts that recorded argument from the filesystem write timestamp of `build_receipt.json`, written at the end. It is an **approximate** launch-to-receipt interval, subject to filesystem timestamp precision and startup overhead, not a profiler's exact CPU time. AOI-03 and AOI-05 ran concurrently, so their elapsed times must not be added as if they were serial. AOI-06 started after the corrected AOI-05/03 launch. Final directory size is a recursive sum of files in that one case directory, not the full stage or raw source collection.

The three final `build_receipt.json` files were reopened and every listed JSON output matched its recorded SHA-256: **6/6** Hat Yai, **8/8** Bang Ban/Sena and **8/8** Rangsit. The source builder SHA-256 in all three is `c617cf941156024c8ffa068ca37038fbb02c4cb116ee6c8d17fc43ab4e5a1b4d`. New `load_finals` checks passed. For AOI-05 and AOI-06, the final cell assignments match the verified baseline for **20,677** and **17,083** fixed population cells respectively. A receipt and matching file hashes establish reproducible candidate computation, not event observation, hospital operation, road passability or downstream FPPS acceptance.

## Bound source and context identities

- The external `stage_copy_receipt.json` SHA-256 is `a59d5d0f14a3e67d54dfa9b370d3fec02dd3ca76a6561a34846017b36b5c9e2b`: 226 source/review/normalized files, 792,099,296 bytes, individually hash-checked. It includes the preserved Mae Sai finals and prior Hat Yai candidate, but excludes all old AOI-03/05/06 study finals. The stage is a new run, not an in-place mutation of the 22 September evidence snapshot.
- All three final receipts bind Thai reporting-unit GeoJSON SHA-256 `013194e107f14258dc51b6bb1bbc3d5df02b7566432b06ea5ee8b827e5ae2a9a`. The OSM PBF SHA-256 is `b7f46018249638413b1d318bc86519f140c27af20cac7c00d42fe03775b7add1`, and WorldPop 2020 count raster SHA-256 is `fb39d85dd150c45c7b25771f29bcd548e611afa0967224ac6d8ca05727230e20`. Their retrieval time is a source inventory timestamp, not the historical event date.
- Hat Yai uses its own November 2025 candidate manifest SHA-256 `c9298b1b950e973e5ab524652eb7c7ba2a44c4822d1ddd545f8e77316c03b709` and AOI geometry SHA-256 `8ca938deddb527ddb9e6a6c54767e771985e5696d353de5ff9f144be056f61de`. It remains an uncalibrated candidate, not a qualified transfer accuracy result.
- AOI-05 geometry SHA-256 is `b548f3e30c88ec0ec5492f5cd4f8dfbd8e87cdcaa3047f5fb0b928ba4c959fcc`. Its new receipt binds baseline walking context `809d0d1be9857ce5a2bec1cc9263d084815106eadde05a24896621a4a9851abb`, hospital-object review `0869fe9b34e283775cb894ceb64852b6ca6b0877f81227b314312f9c52eebc2c`, and internal 10 km routing selection `c72b0f6ede4281f67df079b076942b2233b402a728d93b720db438d7a6bdf5cf`.
- AOI-06 geometry SHA-256 is `0a7d32aab4a94cc88260071312b7f346bfc05914600e33b0df713e57f3f2dee3`. Its new receipt binds baseline walking context `af5520026640cccf1a9209ff92cd6e848ae18d79a697a992f66448a17ad6e086`, hospital-object review `49a66c72bba6e9434d2cff9250697a3025d86061d1fd496a5a404b3ebd5c1b90`, and internal 10 km routing selection `2b18528e3a4656ef02b42fc99212f33b39067b98067441a6bf6d8bcbe061eecc`.

The selection values are the internal canonical identities in the finals receipts, not hashes of the standalone `routing_selection.json` files. Both walking and modelled-vehicle context hashes are recorded separately in each final `build_receipt.json`; downstream export must bind those files and source-object exclusions consistently. `REPRODUCE.md` gives the portable commands and root variables. A new wall-clock timestamp or runtime change can produce a new package hash even when the method is unchanged, so a replay must record its own receipt rather than borrowing these values.

## Invalid first attempt and safe recovery

The first AOI-05 run used builder SHA-256 `66c8311e5aacd322672869cb57325f26eaa0dba4e90f786a006c7c8d4fd1eabb`. It completed and wrote a receipt (historical SHA-256 `034a3fd2929c0754081b526d0b85905088b901ab5d6f36ac2f89376ffe306f91`), but a shared post-bind loop overwrote the restored `subdistrict_id` assignments. A completed receipt therefore **did not pass** the fixed-demand reporting contract. Hat Yai was running while the builder source changed and ended with `ValueError: Implementation changed during computation`, leaving no `build_receipt.json`.

The operator verified both failed output directories remained inside the configured external stage and preserved them under `invalid_attempts/pre-roster-fix/<aoi-id>` without deleting them. AOI-03 and AOI-05 were rebuilt with the corrected builder SHA-256 `c617cf...a1b4d`; AOI-06 ran after the fix. The corrected receipt/file checks and fixed assignments above apply only to the **new** `study_finals/<aoi-id>` directories. Do not use or copy the invalid attempt in a public package or count it as a passed release run. If a replay fails, preserve the failed external output, record the precise error/source version, repair the code, select a new output directory and rerun; do not overwrite an earlier receipt or silently reuse partial files.

## Combined EvidenceLibrary build

The staged `uv run --extra geo python scripts/build_evidence_library.py` run used the verified bundle/reports/context/boundary roots, `--reuse-normalized` and `--require-lower-basin-context`. It began at **2026-09-23T13:25:21.4176612Z** after the three corrected finals and completed successfully. `REPRODUCE.md` gives the full portable flag sequence; the exact original absolute argv was not retained in this resource checkpoint. The public report was written at **13:49:40.843Z** and the last resource sample at **13:49:45.760Z**, approximately **24 min 21 s** and **24 min 24 s** after the launch metadata respectively. These are approximate wall intervals to artifacts/monitor stop, not exact CPU or process-exit time.

The completed `qa/evidence_build_resource_samples.csv` SHA-256 is `06762a916bbbe5a31f4272975dcffb3f527dc231fc95ae37b119b05cedb88a44`, with **277** samples from 13:26:07.989Z to 13:49:45.760Z. The largest sampled process working set was **7,635,927,040 bytes (7.112 GiB)**. Sampling started about 47 seconds after the launch metadata and excludes possible earlier peaks, child processes and system-wide concurrent usage. Virtual size and pagefile usage are different measures; none of these is a minimum hardware requirement.

The staged output has **8 case packages** for **6 AOIs**, package version `3385c9eacccd0d2d`, with **363** source files verified by the builder. The new `public_export_report.json` SHA-256 is `97a9fea41a3184af2b124567623899335454d31e1e693f4217a5e48b6c446bbe`; it lists exactly **24 allowlisted public files**. Their staged directory contains **290,563,073 bytes (277.1 MiB)**, including `catalog.json` SHA-256 `3123d15ba78c1390647b0e27a8bbe75620d49efcdf148108b011929fc7120a7d`. The local `packages/` directory contains eight files totalling **143,251,311 bytes (136.6 MiB)**. A whole-stage snapshot immediately after the build totalled 8,746,848,767 bytes (8.146 GiB), including copied inputs, corrected finals, invalid preserved attempts and QA files; later verifier receipts add a few bytes. This is **not** the incremental build size or an offline-package size.

The stage/current public file-set comparison found matching 24 relative names before publication. The first independent `verify_evidence_library.py` attempt wrote `qa/rebuilt-export-verification.json` (SHA-256 `a0b5be24c15b74d83ea5767c7452560dc3716f9018c9d42a1ea24ef685aa91c3`) with `status=failed`: the strict package schema rejected `routing_policy` and `routing_selection_sha256` under `decision_brief/finals_analysis/case_identity`. The implementation owner corrected the package and decision-brief schemas and TS finals type. A second attempt wrote `qa/rebuilt-export-verification-v2.json` (SHA-256 `be3acc4ecf22f982f877724af4f7707f6472cc4ca7ffec96812a87a7fb233ada`) with `status=failed`: the generic AOI-06 database contained a documented duplicate hospital point marked ineligible, while the verifier had not applied the package's destination-review exclusion in that path. There was **no eligible generic OSM shelter** in the eight staged public databases; the error text covered both conditions. The implementation owner fixed that verifier path, checked the source-bound AOI-06 database directly and passed 62 export-validation and nine contract tests. The third and fourth full attempts are recorded below.

A third full attempt wrote `qa/rebuilt-export-verification-v3.json` (SHA-256 `e5de1a88f87a06d5e367fc66bd2289fb1d0925958218f5c42ef7796a407e10a9`) with `status=failed` at the verifier's **512 MiB uncompressed public database limit**. Streaming byte counts identified only `databases/aoi-06_chao_phraya_rangsit-finals.json.gz` above that limit: **588,903,070 uncompressed bytes (561.6 MiB)** from a 66,436,388-byte GZIP. AOI-05 finals were 390,701,650 uncompressed bytes (372.6 MiB); all other staged databases were smaller. This is a real artifact-size policy and memory boundary, not a corrupt gzip conclusion. The implementation owner raised the verifier's explicit bounded-decompression cap to **640 MiB (671,088,640 bytes)**, still below 1 GiB, and ran a fourth full verification. That engineering guard change does not improve scientific evidence or make the 561.6 MiB browser/download payload light; memory, transport and device constraints need release QA. The fourth result follows; exact final source/profile/device binding remains pending.

The fourth full verifier attempt **passed** at 2026-09-23T14:39:16.567905Z. Its `qa/rebuilt-export-verification-v4.json` SHA-256 is `5c3ec28328ecdc77173b8212e596ad78382fbc196c79f1afa6172e78e9a9221e`, binding catalog SHA-256 `3123d15ba78c1390647b0e27a8bbe75620d49efcdf148108b011929fc7120a7d`, eight packages, 24 allowlisted files and eight GZIP databases. It checked local registry lineage (166 assets, 17 datasets, 47 normalized outputs and 176 supplementary proofs). Its own `source_bytes_reverified=false` field means it did not reopen the immutable source bundle; that separate 363-file intake/source check is recorded in M1. The verifier expressly records `scientific_or_operational_acceptance=false` and `non_operational=true`.

The approved 24-file copy into `apps/web/public/evidence-library` has external `qa/repository-public-copy-receipt.json` SHA-256 `1ad017bb49b8707049c8befce8698713deec28efedf81271e01f69d12463c2e7`. It binds the v4 verifier, prior catalog SHA-256 `e3aa93bd1cbaf21fddc4ce87a0a947a8b1e05c6729451620e962d8ccc65346b2`, new catalog and export-report hashes. An independent rehash found the repository relative-file set exactly matched all **24/24** verifier entries with no extras. The earlier v1-v3 failures remain preserved as corrective evidence. This establishes the candidate public data copy, **not** exact final source, regenerated projections/briefs, profile/offline/Preview or scientific acceptance.

A separate repository public-only `verify_evidence_library.py` run passed at 2026-09-23T15:12:32Z, receipt `qa/repository-public-precommit-verification.json` SHA-256 `5162677e67d38e37506e38d05ce2ac850d17d7a41a28957953c1c9055192fc06`. It rechecked the repository's eight packages, six AOIs, 24 public files and eight GZIP databases against catalog SHA-256 `3123d15ba78c1390647b0e27a8bbe75620d49efcdf148108b011929fc7120a7d`. Its `local_verification=null` is intentional: this scoped run checks public files, while v4 checked local lineage. Both receipts retain `non_operational=true` and scientific/operational acceptance false. The current frontend build subsequently exposed a GZIP artifact-size audit issue for AOI-05/06; a bounded streaming audit and device/profile check remain a distinct release gate.

The first current-catalog frontend build failed at its GZIP audit. A bounded streaming audit subsequently passed 17 asset/auditor tests and both profile builds in partial frontend attempts. A later full-browser attempt repeatedly redrew every service/mode route for eight packages across two viewports, two languages and online/offline; its first AOI-06 desktop pass exceeded ten minutes. The owner safely stopped that attempt with Ctrl-C (exit 1) and confirmed no child process remained. This is a **test-matrix cost observation**, not a measured browser memory peak or product failure. The bounded replacement's offline-only run passed in about 77 seconds; its final default browser run passed in about three minutes. The latter checked 64 brief selections, 32 shared-view selections, 33 exact routes, 16 workspace combinations, keyboard/details, invalid links/downloads and scenario/version deep links. Four services/two modes were represented, while only AOI-01/03/06 hospital-walking routes received a full origin/change matrix. Complete precommit `pnpm verify:frontend` then passed exit 0 in about 9.14 minutes; its durable external log `qa/frontend-verify-precommit-attempt4.log` SHA-256 is `1f879feee5eaf5c4e8ab2d31301b780d058b31ef3309c901d941e5aa2783969e`. The streaming helper's working set was sampled up to about 188 MiB over 40 seconds, not a formal peak or AOI-06-specific browser measure. Exact-source replay and device memory risk remain open.

## Second-machine boundary

There has been **no real second-machine or another-person offline rehearsal**. Its status is **NOT RUN** even if local tests, browser offline smoke, ZIP hash verification and this laptop's local server pass. Another machine must use copied manifests/external roots, locked dependencies and the extracted offline ZIP; record its actual hardware, source/ZIP hashes, commands, observations and any mismatches. Do not claim portability or operational acceptance from this single-laptop run.
