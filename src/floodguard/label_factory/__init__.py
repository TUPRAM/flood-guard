"""FloodGuard's report-only, human-in-the-loop flood-label factory.

Submodules are intentionally imported explicitly so optional ML dependencies
are never loaded by importing :mod:`floodguard.label_factory` itself.
"""

